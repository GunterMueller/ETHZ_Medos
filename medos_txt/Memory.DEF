DEFINITION MODULE Memory;   (*NW 21.4.82*)
  FROM SYSTEM IMPORT ADDRESS;

  EXPORT QUALIFIED Init, New, Mark, Reset;

  PROCEDURE Init;
  PROCEDURE New(n: CARDINAL): ADDRESS;
  PROCEDURE Mark;
  PROCEDURE Reset;
END Memory.
